# 使用方法ガイド

このドキュメントでは、Googleマップ店舗情報取得ツールの使用方法を説明します。

## 前提条件

[セットアップガイド](./SETUP.md)に従って、以下の準備が完了していることを確認してください：

- ✅ Google Cloud Platformのプロジェクト作成
- ✅ サービスアカウントの作成とJSONキーのダウンロード
- ✅ Google Sheets APIの有効化
- ✅ 入力用スプレッドシートの作成と権限設定

---

## ステップ1: Google Colabでノートブックを開く

### 1-1. ノートブックをアップロード

1. https://colab.research.google.com/ にアクセス
2. 「ファイル」→「ノートブックをアップロード」を選択
3. `google_maps_scraper.ipynb` ファイルを選択してアップロード

または、GitHubから直接開く場合：
1. 「ファイル」→「ノートブックを開く」
2. 「GitHub」タブを選択
3. リポジトリURLまたはファイルパスを入力

### 1-2. ランタイムの設定

1. 「ランタイム」→「ランタイムのタイプを変更」を選択
2. ハードウェアアクセラレータ: 「なし」（GPUは不要）
3. 「保存」をクリック

---

## ステップ2: セットアップセルの実行

### 2-1. ライブラリのインストール

最初のコードセル（環境セットアップ）を実行します：

```python
!pip install playwright gspread oauth2client pandas openpyxl -q
!playwright install chromium
!playwright install-deps chromium
```

▶️ セルの左側の実行ボタンをクリック、または `Shift + Enter` で実行

⏱️ 初回は数分かかる場合があります。

---

## ステップ3: 認証情報のアップロード

### 3-1. サービスアカウントJSONのアップロード

認証設定のセルを実行します：

```python
from google.colab import files
...
```

▶️ 実行すると「ファイルを選択」ボタンが表示されます

1. 「ファイルを選択」ボタンをクリック
2. セットアップ時にダウンロードした **サービスアカウントのJSONファイル** を選択
3. アップロード完了を確認

✅ 「✓ xxxxx.json をアップロードしました」と表示されればOK

---

## ステップ4: 設定の入力

### 4-1. スプレッドシートURLの設定

設定セルで、入力用スプレッドシートのURLを貼り付けます：

```python
# 入力用スプレッドシートのURL
INPUT_SHEET_URL = "YOUR_SPREADSHEET_URL_HERE"  # ここを変更
```

**変更例:**
```python
INPUT_SHEET_URL = "https://docs.google.com/spreadsheets/d/1AbCdEfGhIjKlMnOpQrStUvWxYz/edit"
```

### 4-2. その他の設定（オプション）

必要に応じて以下の設定を変更できます：

```python
# スクレイピング設定
START_RANK = 41   # 取得開始順位（デフォルト: 41位から）
END_RANK = 200    # 取得終了順位（デフォルト: 200位まで）
HEADLESS = False  # False=ブラウザ表示、True=非表示

# 待機時間設定（秒）- ブロック対策
MIN_WAIT = 1.5    # 最小待機時間
MAX_WAIT = 3.5    # 最大待機時間
```

**推奨設定:**
- `HEADLESS = False`: ブラウザの動作を確認できるため、初回は推奨
- ブロックされる場合は、待機時間を長くする（例: `MIN_WAIT = 3`, `MAX_WAIT = 6`）

▶️ 設定を変更したら、セルを実行

---

## ステップ5: メイン処理の実行

### 5-1. 関数定義セルの実行

「4. メイン処理」セクションの関数定義セルを実行します。

▶️ 「✓ 関数定義完了」と表示されればOK

### 5-2. スクレイピング実行

「5. 実行」セクションのメイン実行セルを実行します：

```python
await main()
```

---

## 実行中の動作

実行が開始されると、以下のような流れで処理が進みます：

### フェーズ1: 初期化
```
============================================================
Googleマップ店舗情報取得ツール 実行開始
============================================================

入力シートから検索キーワードを読み込み中...
✓ 8件の検索キーワードを読み込みました
```

### フェーズ2: 各キーワードの処理
```
[1/8] 京都 居酒屋 の取得を開始
============================================================
検索キーワード: 京都 居酒屋
取得範囲: 41位 〜 200位
============================================================

Googleマップにアクセス中...
検索完了
スクロールして200件の店舗を読み込み中...
現在の取得数: 20件
現在の取得数: 45件
...
✓ 目標件数 200件に到達しました

取得可能な店舗数: 200件
詳細情報を取得する店舗数: 160件

[41/200] 店舗情報取得中...
  ✓ 居酒屋 さくら
[42/200] 店舗情報取得中...
  ✓ 海鮮居酒屋 まぐろ亭
...
```

### フェーズ3: 完了とスプレッドシート保存
```
============================================================
全キーワードの取得完了
総取得件数: 1280件
============================================================

============================================================
✓ スプレッドシートを作成しました
タイトル: 店舗リスト_20250115_143022
URL: https://docs.google.com/spreadsheets/d/xxxxxxxxxxxxx
============================================================
```

**✅ 完了!** 表示されたURLをクリックして、取得データを確認してください。

---

## 取得データの形式

出力されるスプレッドシートには、以下のカラムが含まれます：

| カラム名 | 説明 | 例 |
|----------|------|-----|
| 検索キーワード | 検索に使用したキーワード | 京都 居酒屋 |
| 順位 | 検索結果での順位 | 45 |
| 店舗名 | 店舗の名前 | 居酒屋 さくら |
| カテゴリー | 業種・カテゴリー | 居酒屋 |
| 評価 | 星の評価 | 4.2 |
| レビュー数 | レビューの件数 | 150 |
| 住所 | 店舗の住所 | 京都府京都市中京区... |
| 電話番号 | 電話番号 | 075-123-4567 |
| WebサイトURL | 公式サイトURL | https://example.com |
| 営業時間 | 営業時間情報 | 営業時間: 月曜日 17時00分～0時00分... |

---

## 実行時間の目安

取得件数や待機時間の設定によって変わりますが、おおよその目安：

- **1キーワード（41〜200位の160件）**: 約10〜20分
- **8キーワード**: 約1.5〜3時間

⏱️ **注意**: 長時間の実行になるため、Google Colabのセッションが切れないよう、ブラウザを開いたままにしておくことを推奨します。

---

## トラブルシューティング

### ❌ エラー: 認証に失敗しました

**原因:**
- サービスアカウントのJSONファイルが正しくアップロードされていない
- Google Sheets APIが有効になっていない

**対処法:**
1. JSONファイルを再アップロード
2. Google Cloud ConsoleでGoogle Sheets APIが有効か確認
3. [セットアップガイド](./SETUP.md)の手順を再確認

---

### ❌ エラー: スプレッドシートにアクセスできません

**原因:**
- サービスアカウントに入力シートの編集権限がない

**対処法:**
1. 入力シートを開く
2. 「共有」ボタンをクリック
3. サービスアカウントのメールアドレス（JSONファイル内の `client_email`）を追加
4. 権限を「編集者」に設定

---

### ⚠️ 警告: スクロール要素が見つかりませんでした

**原因:**
- Googleマップのページ構造が変更された
- ページの読み込みが完了していない

**対処法:**
1. もう一度実行してみる
2. `HEADLESS = False` に設定してブラウザの動作を確認
3. 待機時間を長くする

---

### ⚠️ 取得件数が少ない

**原因:**
- 検索結果がそもそも少ない
- Googleにブロックされた可能性

**対処法:**
1. 待機時間を長くする（`MIN_WAIT = 3`, `MAX_WAIT = 6` など）
2. しばらく時間を置いてから再実行
3. 取得範囲を狭める（`END_RANK = 100` など）
4. `HEADLESS = False` にしてブラウザの動作を確認

---

### 🚫 Googleにブロックされた場合

**症状:**
- CAPTCHAが表示される
- 「unusual traffic」というメッセージが表示される
- 検索結果が取得できない

**対処法:**
1. **待機時間を増やす**
   ```python
   MIN_WAIT = 5
   MAX_WAIT = 10
   ```

2. **取得件数を減らす**
   ```python
   END_RANK = 100  # 200→100に変更
   ```

3. **時間を置く**
   - 数時間〜1日待ってから再実行

4. **IPアドレスを変える**
   - 別のネットワーク環境から実行
   - VPNを使用（推奨しません）

---

## よくある質問（FAQ）

### Q1: Google Colabは無料で使えますか？

**A:** はい、無料で使用できます。ただし、長時間の実行には制限があるため、無料版では12時間程度でセッションが切れる可能性があります。

---

### Q2: 複数回実行すると、過去のデータは上書きされますか？

**A:** いいえ、実行ごとに新しいスプレッドシートが作成されるため、過去のデータは保持されます。

---

### Q3: 取得したデータをCSVファイルでダウンロードできますか？

**A:** はい、スプレッドシートを開いて、「ファイル」→「ダウンロード」→「カンマ区切り形式（.csv）」を選択してください。

---

### Q4: エリアや業種を追加したい

**A:** 入力用スプレッドシートに行を追加するだけでOKです。次回実行時に自動的に反映されます。

---

### Q5: 1位〜40位のデータも取得したい

**A:** 設定セルで `START_RANK = 1` に変更してください。

---

### Q6: スクレイピングは違法ではないですか？

**A:** Googleマップの利用規約では自動アクセスが制限されています。このツールは教育目的で提供されており、大量のリクエストや商用利用には適していません。利用は自己責任でお願いします。

---

## ヒントとベストプラクティス

### 💡 ヒント1: 少量でテスト実行
初めて使う場合は、入力シートのキーワードを1〜2件に減らしてテスト実行することを推奨します。

### 💡 ヒント2: 待機時間は長めに
ブロックを避けるため、`MIN_WAIT` と `MAX_WAIT` は長めに設定することを推奨します（特に大量取得時）。

### 💡 ヒント3: ブラウザ表示モード
初回は `HEADLESS = False` でブラウザの動作を確認することで、エラーの原因を特定しやすくなります。

### 💡 ヒント4: 定期的なバックアップ
長時間の実行中にエラーが発生する可能性があるため、重要なデータは定期的にダウンロードしてバックアップしてください。

---

## サポート

問題が解決しない場合は、以下の情報を添えてお問い合わせください：

- 実行時のエラーメッセージ（スクリーンショット）
- 使用した設定（`INPUT_SHEET_URL`、`START_RANK`、`END_RANK`など）
- 入力シートの内容（スクリーンショット）

---

## 次のステップ

取得したデータを活用して、MEOやSNS事業を成功させてください！

データの分析や活用方法についてご質問があれば、お気軽にお問い合わせください。
