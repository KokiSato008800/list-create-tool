# Google Cloud Platform 設定手順（超詳細版）

このガイドでは、初めての方でも迷わないように、**画像なしでも分かる詳細な手順**でGoogle Cloud Platformの設定を説明します。

---

## 📋 この作業で何をするのか？

Googleスプレッドシートにアクセスするための「鍵」を作成します。
この鍵があれば、Google Colabからスプレッドシートを読み書きできるようになります。

---

## ステップ1: Google Cloud Consoleにアクセス

### 1-1. ブラウザで以下のURLを開く

```
https://console.cloud.google.com/
```

### 1-2. Googleアカウントでログイン

- 普段使っているGoogleアカウントでログインしてください
- ログイン済みの場合は、そのまま次へ

---

## ステップ2: 新しいプロジェクトを作成

### 2-1. プロジェクト選択画面を開く

画面の**左上**に「Google Cloud」というロゴがあります。
その右側に「▼」マークがついた部分をクリックしてください。

**探す場所:**
- 画面の一番上、左端
- 青い文字で「プロジェクトを選択」と書かれているはず

### 2-2. 「新しいプロジェクト」をクリック

ポップアップウィンドウが開きます。
右上に「新しいプロジェクト」ボタンがあるので、クリックしてください。

### 2-3. プロジェクト名を入力

**プロジェクト名:**
```
google-maps-scraper
```
（好きな名前でOKですが、英語推奨）

**場所:**
- 「場所」は「組織なし」のままでOK

**ボタンをクリック:**
- 右下の「作成」ボタンをクリック

### 2-4. プロジェクト作成完了を待つ

- 画面右上に「プロジェクトを作成しています...」と表示されます
- **30秒〜1分程度**待ちます
- 完了すると「プロジェクトが作成されました」と通知が表示されます

### 2-5. 作成したプロジェクトを選択

- 画面左上の「プロジェクトを選択」をもう一度クリック
- 先ほど作成した「google-maps-scraper」をクリックして選択
- 画面左上に「google-maps-scraper」と表示されていればOK

---

## ステップ3: Google Sheets APIを有効にする

### 3-1. APIライブラリを開く

画面左側に**ハンバーガーメニュー（≡ 三本線）**があります。
これをクリックして、以下の順で進んでください:

1. **「APIとサービス」** にマウスを合わせる（クリックしない）
2. サブメニューが開くので **「ライブラリ」** をクリック

**探し方のコツ:**
- 左側のメニューを上から下にスクロール
- 「APIとサービス」は上の方にあります

### 3-2. Google Sheets APIを検索

**検索ボックスに入力:**
```
Google Sheets API
```

- 画面上部に検索ボックスがあります
- 入力すると、候補が表示されます

### 3-3. Google Sheets APIをクリック

検索結果に「Google Sheets API」が表示されるので、クリックしてください。

**見た目:**
- 緑色のスプレッドシートアイコン
- 「Google Sheets API」という名前

### 3-4. 「有効にする」ボタンをクリック

- 青いボタン「有効にする」をクリック
- 数秒待つと、「API が有効になりました」と表示されます

### 3-5. Google Drive APIも有効にする（推奨）

同じ手順で **Google Drive API** も有効にしてください:

1. 左上の「← APIとサービス」をクリックして戻る
2. 「ライブラリ」をクリック
3. 検索ボックスに `Google Drive API` と入力
4. 「Google Drive API」をクリック
5. 「有効にする」をクリック

---

## ステップ4: サービスアカウントを作成

### 4-1. サービスアカウントページを開く

**左側のハンバーガーメニュー（≡）をクリック** → 以下の順で進む:

1. **「IAMと管理」** にマウスを合わせる
2. サブメニューから **「サービス アカウント」** をクリック

### 4-2. 「サービス アカウントを作成」をクリック

- 画面上部に **「+ サービス アカウントを作成」** ボタンがあります
- このボタンをクリック

**注意:**
初めて使う場合、「APIを有効にしてください」と表示される場合があります。
その場合は、前のステップ（APIの有効化）を確認してください。

### 4-3. サービスアカウントの詳細を入力

**画面1/3: サービスアカウントの詳細**

**サービスアカウント名:**
```
sheets-scraper
```
（任意の名前でOK）

**サービスアカウントID:**
- 自動で入力されます（変更不要）

**説明（オプション）:**
```
Googleマップスクレイピング用
```

**ボタンをクリック:**
- **「作成して続行」** をクリック

### 4-4. ロール（権限）を付与

**画面2/3: サービスアカウントへのアクセス権の付与**

**ロールを選択:**
1. 「ロールを選択」の入力ボックスをクリック
2. 検索ボックスに `編集者` と入力
3. **「基本」→「編集者」** を選択

**見た目:**
- 「基本」カテゴリの中に「編集者（Editor）」があります

**ボタンをクリック:**
- **「続行」** をクリック

### 4-5. ユーザーアクセスをスキップ

**画面3/3: ユーザーにこのサービスアカウントへのアクセスを許可**

- 何も入力せずに **「完了」** をクリック

---

## ステップ5: JSONキーファイルを作成・ダウンロード

### 5-1. サービスアカウント一覧を表示

「サービス アカウント」ページに戻ります。
作成した **sheets-scraper@...** というアカウントが一覧に表示されています。

### 5-2. サービスアカウントをクリック

**sheets-scraper@xxxxx.iam.gserviceaccount.com** というメールアドレスをクリックしてください。

### 5-3. 「キー」タブを開く

画面上部に複数のタブがあります:
- 詳細
- 権限
- **キー** ← これをクリック
- ログ
- 指標

### 5-4. 新しいキーを作成

**「鍵を追加」** ボタンをクリック → **「新しい鍵を作成」** を選択

### 5-5. JSON形式を選択

ポップアップウィンドウが表示されます。

**キーのタイプを選択:**
- ◉ **JSON**（選択済みのはず）
- ○ P12

**ボタンをクリック:**
- **「作成」** をクリック

### 5-6. JSONファイルが自動ダウンロード

- 自動的にJSONファイルがダウンロードされます
- ファイル名の例: `google-maps-scraper-xxxxxxxxxxxxx.json`

**⚠️ 重要:**
- このファイルは**絶対に誰にも共有しないでください**
- パスワードと同じくらい重要な機密情報です
- 安全な場所に保存してください（デスクトップやダウンロードフォルダでOK）

---

## ステップ6: 入力用Googleスプレッドシートを作成

### 6-1. Googleスプレッドシートを開く

ブラウザで以下のURLを開く:
```
https://sheets.google.com/
```

### 6-2. 新しいスプレッドシートを作成

- **「空白」** をクリック
- 新しいスプレッドシートが開きます

### 6-3. スプレッドシートに以下のデータを入力

**1行目（ヘッダー）:**

| A列 | B列 | C列 | D列 |
|-----|-----|-----|-----|
| エリア | 業種 | 開始順位 | 終了順位 |

**2行目以降（データ）:**

| A列 | B列 | C列 | D列 |
|-----|-----|-----|-----|
| 京都 | 居酒屋 | 41 | 200 |
| 京都 | 焼肉 | 41 | 200 |
| 京都 | 海鮮 | 41 | 200 |
| 金沢 | 居酒屋 | 41 | 200 |
| 金沢 | 焼肉 | 41 | 200 |
| 金沢 | 海鮮 | 41 | 200 |
| 博多 | 居酒屋 | 41 | 200 |
| 博多 | 焼肉 | 41 | 200 |

**入力のコツ:**
- A1セルに「エリア」と入力
- B1セルに「業種」と入力
- C1セルに「開始順位」と入力
- D1セルに「終了順位」と入力
- A2セルから順番にデータを入力

### 6-4. スプレッドシートに名前をつける

- 画面左上の「無題のスプレッドシート」をクリック
- 名前を入力（例: `店舗リスト入力シート`）

---

## ステップ7: サービスアカウントに権限を付与

**これが一番重要です！**

### 7-1. ダウンロードしたJSONファイルを開く

- 先ほどダウンロードしたJSONファイルをテキストエディタで開く
  - Macの場合: 右クリック → 「このアプリケーションで開く」 → 「テキストエディット」
  - Windowsの場合: 右クリック → 「プログラムから開く」 → 「メモ帳」

### 7-2. `client_email` の値をコピー

JSONファイルの中に以下のような行があります:

```json
{
  "type": "service_account",
  "project_id": "google-maps-scraper-xxxxx",
  "private_key_id": "xxxxxxxxxxxxx",
  "private_key": "-----BEGIN PRIVATE KEY-----\n...",
  "client_email": "sheets-scraper@google-maps-scraper-xxxxx.iam.gserviceaccount.com",
  ...
}
```

**`"client_email"` の値（メールアドレス）をコピーしてください。**

例:
```
sheets-scraper@google-maps-scraper-xxxxx.iam.gserviceaccount.com
```

**コピーのコツ:**
- ダブルクォーテーション `"` は含めない
- メールアドレス部分だけをコピー

### 7-3. スプレッドシートの共有設定を開く

Googleスプレッドシート（先ほど作成した入力シート）に戻ります。

**画面右上の「共有」ボタンをクリック**
- 青いボタンです
- 「共有」または「Share」と書かれています

### 7-4. サービスアカウントのメールアドレスを追加

**「ユーザーやグループを追加」の入力ボックスに:**
- 先ほどコピーしたメールアドレスを貼り付け
- 例: `sheets-scraper@google-maps-scraper-xxxxx.iam.gserviceaccount.com`

**権限を設定:**
- 右側のドロップダウンをクリック
- **「編集者」** を選択

**「送信」ボタンをクリック**

**ポップアップが表示されたら:**
- 「通知を送信しますか？」→ **「通知しない」** でOK
- （サービスアカウントはメールを受け取りません）

### 7-5. スプレッドシートのURLをコピー

ブラウザのアドレスバーに表示されているURLをコピーしてください。

**URL例:**
```
https://docs.google.com/spreadsheets/d/1AbCdEfGhIjKlMnOpQrStUvWxYz_1234567890/edit
```

**このURLは後でGoogle Colabで使います！**

---

## ✅ 事前準備完了チェックリスト

以下が全て揃っていれば、Google Colabで実行できます:

- [ ] Google Cloud Platformでプロジェクトを作成した
- [ ] Google Sheets APIを有効にした
- [ ] Google Drive APIを有効にした
- [ ] サービスアカウントを作成した
- [ ] JSONキーファイルをダウンロードした
- [ ] 入力用スプレッドシートを作成した
- [ ] スプレッドシートにサービスアカウントの編集権限を付与した
- [ ] スプレッドシートのURLをコピーした

---

## 🎯 次のステップ

これで事前準備は完了です！

次は **Google Colab** で以下の手順を実行してください:

### Google Colabでの作業

1. `google_maps_scraper.ipynb` をGoogle Colabで開く
2. **セル1（ライブラリインストール）** を実行
3. **セル2（認証設定）** を実行
   - 「ファイルを選択」ボタンが表示される
   - ダウンロードしたJSONファイルをアップロード
4. **セル3（設定）** でスプレッドシートURLを貼り付け
5. **残りのセルを順番に実行**

詳しくは [USAGE.md](./USAGE.md) を参照してください。

---

## ❓ よくある質問

### Q1: JSONファイルをなくしました

**A:** 以下の手順で再作成できます:
1. Google Cloud Console → サービスアカウント
2. 作成したサービスアカウントをクリック
3. 「キー」タブ → 「鍵を追加」 → 「新しい鍵を作成」
4. JSON形式で作成

**注意:** 古いキーは無効になるので、新しいキーを使ってください。

### Q2: サービスアカウントのメールアドレスが見つかりません

**A:**
- Google Cloud Console → サービスアカウント ページで確認できます
- `xxxxx@xxxxx.iam.gserviceaccount.com` という形式です
- または、JSONファイルの `client_email` を確認

### Q3: スプレッドシートの権限設定がうまくいきません

**A:**
- メールアドレスを正確にコピーしているか確認
- ダブルクォーテーション `"` が含まれていないか確認
- 権限が「編集者」になっているか確認

### Q4: Google Cloud Consoleの画面が違います

**A:**
- Googleは画面デザインを頻繁に変更します
- 基本的な流れは同じなので、似た項目を探してください
- メニュー名が少し違う場合がありますが、「サービスアカウント」「APIライブラリ」などのキーワードで検索してください

---

これで事前準備は完璧です！分からない点があれば、いつでも質問してください。
