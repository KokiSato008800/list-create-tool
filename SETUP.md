# セットアップガイド

このドキュメントでは、Googleマップ店舗情報取得ツールを使用するための事前準備手順を説明します。

## 目次
1. [Google Cloud Platform（GCP）の設定](#1-google-cloud-platformgcpの設定)
2. [サービスアカウントの作成](#2-サービスアカウントの作成)
3. [Google Sheets APIの有効化](#3-google-sheets-apiの有効化)
4. [入力用スプレッドシートの準備](#4-入力用スプレッドシートの準備)

---

## 1. Google Cloud Platform（GCP）の設定

### 1-1. GCPコンソールにアクセス
1. https://console.cloud.google.com/ にアクセス
2. Googleアカウントでログイン

### 1-2. 新しいプロジェクトを作成
1. 画面上部の「プロジェクトを選択」をクリック
2. 「新しいプロジェクト」をクリック
3. プロジェクト名を入力（例: `google-maps-scraper`）
4. 「作成」をクリック
5. 作成したプロジェクトを選択

---

## 2. サービスアカウントの作成

### 2-1. サービスアカウントページへ移動
1. 左側メニューから「IAMと管理」→「サービス アカウント」を選択
2. 「サービス アカウントを作成」をクリック

### 2-2. サービスアカウント情報の入力
1. **サービスアカウント名**: `sheets-scraper`（任意の名前）
2. **サービスアカウントID**: 自動生成されます
3. **説明**: 「Googleマップスクレイピング用」など
4. 「作成して続行」をクリック

### 2-3. ロールの付与
1. 「ロールを選択」で「基本」→「編集者」を選択
2. 「続行」をクリック
3. 「完了」をクリック

### 2-4. JSONキーの作成とダウンロード
1. 作成したサービスアカウントの一覧から、先ほど作成したアカウントをクリック
2. 「キー」タブを選択
3. 「鍵を追加」→「新しい鍵を作成」をクリック
4. 「JSON」を選択
5. 「作成」をクリック

**→ JSONファイルが自動的にダウンロードされます。このファイルは後で使用するので、安全な場所に保存してください。**

⚠️ **重要**: このJSONファイルには機密情報が含まれています。他人と共有したり、GitHubなどに公開しないでください。

---

## 3. Google Sheets APIの有効化

### 3-1. APIライブラリへ移動
1. 左側メニューから「APIとサービス」→「ライブラリ」を選択

### 3-2. Google Sheets APIを有効化
1. 検索ボックスに「Google Sheets API」と入力
2. 「Google Sheets API」をクリック
3. 「有効にする」をクリック

### 3-3. Google Drive APIを有効化（オプションだが推奨）
1. 同様に検索ボックスに「Google Drive API」と入力
2. 「Google Drive API」をクリック
3. 「有効にする」をクリック

---

## 4. 入力用スプレッドシートの準備

### 4-1. 新しいGoogleスプレッドシートを作成
1. https://sheets.google.com にアクセス
2. 「空白」をクリックして新しいスプレッドシートを作成

### 4-2. 入力シートの構成

以下の形式でスプレッドシートを作成してください：

| エリア | 業種 | 開始順位 | 終了順位 |
|--------|------|----------|----------|
| 京都   | 居酒屋 | 41     | 200      |
| 京都   | 焼肉 | 41       | 200      |
| 京都   | 海鮮 | 41       | 200      |
| 金沢   | 居酒屋 | 41     | 200      |
| 金沢   | 焼肉 | 41       | 200      |
| 金沢   | 海鮮 | 41       | 200      |
| 博多   | 居酒屋 | 41     | 200      |
| 博多   | 焼肉 | 41       | 200      |

**カラムの説明:**
- **エリア**: 検索する地域名
- **業種**: 検索する業種・カテゴリー
- **開始順位**: 取得を開始する順位（デフォルト: 41）
- **終了順位**: 取得を終了する順位（デフォルト: 200）

⚠️ **注意**:
- 1行目は必ずヘッダー行にしてください
- カラム名は上記の通り正確に入力してください

### 4-3. サービスアカウントに権限を付与

1. スプレッドシート右上の「共有」ボタンをクリック
2. 先ほどダウンロードしたJSONファイルを開く
3. JSONファイル内の `"client_email"` の値（`xxxxx@xxxxx.iam.gserviceaccount.com` のようなメールアドレス）をコピー
4. スプレッドシートの共有画面に、コピーしたメールアドレスを貼り付け
5. 権限を「編集者」に設定
6. 「送信」をクリック（通知メールは送信しなくてOK）

### 4-4. スプレッドシートURLをコピー

ブラウザのアドレスバーからスプレッドシートのURLをコピーしてください。
このURLは後でGoogle Colabノートブックで使用します。

例: `https://docs.google.com/spreadsheets/d/XXXXXXXXXXXXXXXXXXXXXXXXXXXXXX/edit`

---

## 次のステップ

セットアップが完了したら、[使用方法ガイド](./USAGE.md)を参照して、Google Colabでスクレイピングを実行してください。

---

## トラブルシューティング

### Q: サービスアカウントが作成できない
A: GCPプロジェクトが正しく選択されているか確認してください。また、Googleアカウントに請求先情報が登録されている必要がある場合があります。

### Q: APIが有効化できない
A: プロジェクトが選択されているか確認してください。初めてAPIを有効化する場合、請求先アカウントの設定が必要な場合があります（ただし、Google Sheets APIは無料枠内で使用できます）。

### Q: スプレッドシートにアクセスできないエラーが出る
A: サービスアカウントのメールアドレスに、スプレッドシートの編集権限が付与されているか確認してください。

---

## セキュリティに関する注意事項

- サービスアカウントのJSONキーファイルは、パスワードと同様に機密情報です
- JSONファイルを他人と共有しない
- GitHubやその他の公開リポジトリにアップロードしない
- 使用しなくなったキーは削除する
- 定期的にキーをローテーション（再作成）することを推奨
