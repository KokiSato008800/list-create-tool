# 🚀 Streamlit Cloud デプロイ手順

このガイドでは、作成したStreamlitアプリをStreamlit Cloudにデプロイする手順を説明します。

---

## 📋 事前準備

以下が必要です：

- ✅ GitHubアカウント
- ✅ Google Cloud Platformのサービスアカウント（既に作成済み）
- ✅ このプロジェクトのファイル一式

---

## ステップ1: GitHubリポジトリの作成

### 1-1. GitHub

にログイン

https://github.com にアクセスしてログインします。

### 1-2. 新しいリポジトリを作成

1. 右上の「+」ボタン → 「New repository」をクリック
2. 以下の情報を入力：
   - **Repository name**: `google-maps-scraper`（任意の名前）
   - **Description**: 「Googleマップ店舗情報取得ツール」
   - **Public/Private**: 「Private」を推奨（認証情報が含まれるため）
3. 「Create repository」をクリック

### 1-3. ローカルファイルをGitHubにアップロード

**方法A: GitHub Web UI（簡単）**

1. 作成したリポジトリページを開く
2. 「uploading an existing file」をクリック
3. 以下のファイルをドラッグ&ドロップ：
   - `streamlit_app.py`
   - `scraper.py`
   - `sheets_handler.py`
   - `config.py`
   - `requirements.txt`
   - `.streamlit/config.toml`
4. 「Commit changes」をクリック

**方法B: Git CLI（上級者向け）**

```bash
cd /Users/satokoki/projects/その他/list_create_tool

git init
git add streamlit_app.py scraper.py sheets_handler.py config.py requirements.txt .streamlit/
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/google-maps-scraper.git
git push -u origin main
```

---

## ステップ2: Streamlit Cloudの設定

### 2-1. Streamlit Cloudにアクセス

https://share.streamlit.io/ にアクセスします。

### 2-2. GitHubアカウントでサインアップ

1. 「Sign up」または「Continue with GitHub」をクリック
2. GitHubアカウントでログイン
3. Streamlit Cloudがリポジトリにアクセスする権限を許可

### 2-3. 新しいアプリをデプロイ

1. 「New app」ボタンをクリック
2. 以下の情報を入力：

   - **Repository**: `your-username/google-maps-scraper`を選択
   - **Branch**: `main`
   - **Main file path**: `streamlit_app.py`

3. **Advanced settings** をクリック（重要！）

---

## ステップ3: Playwright設定（重要）

### 3-1. packages.txtファイルの作成

Streamlit Cloudでは、Playwrightの依存関係を別途インストールする必要があります。

GitHubリポジトリに以下のファイルを追加してください：

**ファイル名**: `packages.txt`

**内容**:
```
chromium
chromium-driver
```

### 3-2. 起動スクリプトの追加

**ファイル名**: `.streamlit/secrets.toml`は不要ですが、代わりに以下を実行：

Streamlit Cloudの「Advanced settings」→「Secrets」に以下を追加：

```toml
# 何も追加しなくてOK（config.pyに認証情報が含まれているため）
```

---

## ステップ4: Playwrightインストール設定

Streamlit Cloudでは、アプリ起動時にPlaywrightブラウザをインストールする必要があります。

### 4-1. requirements.txtの更新

`requirements.txt`に以下を追加：

```
streamlit==1.31.0
playwright==1.41.0
gspread==5.12.4
oauth2client==4.1.3
pandas==2.1.4
openpyxl==3.1.2
```

### 4-2. インストールスクリプトの追加（オプション）

GitHubリポジトリに以下のファイルを作成：

**ファイル名**: `.streamlit/install.sh`

**内容**:
```bash
#!/bin/bash
playwright install chromium
playwright install-deps chromium
```

---

## ステップ5: デプロイ実行

### 5-1. デプロイボタンをクリック

Streamlit Cloudの設定画面で「Deploy!」ボタンをクリックします。

### 5-2. ビルドログを確認

デプロイが開始されます。以下のログが表示されます：

```
Installing dependencies...
Installing Playwright...
Starting app...
```

⏱️ 初回デプロイは5〜10分かかります。

### 5-3. デプロイ完了

デプロイが完了すると、以下のようなURLが表示されます：

```
https://your-app-name.streamlit.app
```

このURLをブラウザで開いて、アプリが正常に動作するか確認してください。

---

## ⚠️ トラブルシューティング

### エラー1: Playwrightのインストールに失敗

**症状**:
```
ModuleNotFoundError: No module named 'playwright'
```

**対処法**:
1. `requirements.txt`に`playwright==1.41.0`が含まれているか確認
2. Streamlit Cloudの「Reboot app」をクリック

---

### エラー2: Chromiumブラウザが見つからない

**症状**:
```
Executable doesn't exist at /ms-playwright/chromium-*/chrome-linux/chrome
```

**対処法**:
1. `packages.txt`ファイルを作成（上記参照）
2. 以下を追加：
   ```
   chromium
   chromium-driver
   ```
3. GitHubにプッシュ
4. Streamlit Cloudが自動的に再デプロイ

---

### エラー3: Google Sheets APIエラー

**症状**:
```
gspread.exceptions.APIError: PERMISSION_DENIED
```

**対処法**:
1. サービスアカウントのメールアドレス（`sheets-scraper@list-create-tool.iam.gserviceaccount.com`）を確認
2. 入力用スプレッドシートの共有設定で、このアドレスに「編集者」権限を付与
3. Google Sheets APIがGCP で有効になっているか確認

---

### エラー4: タイムアウトエラー

**症状**:
アプリが起動しない、または実行中にタイムアウトする

**対処法**:
1. Streamlit Cloudの無料プランでは制限があります
2. 取得範囲を狭める（例: 41〜100位）
3. 検索キーワード数を減らす

---

## 🔧 デプロイ後の更新方法

### コードを更新した場合

1. GitHubリポジトリのファイルを更新（Web UIまたはGit CLI）
2. Streamlit Cloudが**自動的に再デプロイ**します
3. 数分待つと更新が反映されます

### 手動で再デプロイする場合

1. Streamlit Cloudのダッシュボードを開く
2. アプリの「⋮」メニューをクリック
3. 「Reboot app」を選択

---

## 📊 リソース制限（無料プラン）

Streamlit Cloudの無料プランには以下の制限があります：

- **アプリ数**: 3アプリまで
- **RAM**: 1GB
- **CPU**: 共有
- **実行時間**: 特に制限なし（ただし無アクセス時にスリープ）

**推奨**:
- 大量のデータ取得には向いていません
- 1回の実行で1,000〜2,000件程度が目安

---

## 🔐 セキュリティ設定

### リポジトリをPrivateにする

認証情報（`config.py`）が含まれるため、GitHubリポジトリは**Private**に設定することを推奨します。

1. GitHubリポジトリの「Settings」を開く
2. 「Danger Zone」→「Change repository visibility」
3. 「Make private」を選択

### アクセス制限

Streamlit Cloudで作成されたアプリは、URLを知っていれば誰でもアクセスできます。

アクセスを制限したい場合：
1. Streamlit Cloudのダッシュボードでアプリを選択
2. 「Settings」→「Sharing」
3. 「Require viewers to log in」を有効化（有料プランのみ）

---

## 🎉 デプロイ完了！

これでStreamlit Cloudへのデプロイが完了しました。

**次のステップ**:
1. アプリのURLをチームに共有
2. [使い方マニュアル](./使い方マニュアル.md)を共有
3. 実際に使ってみる

問題が発生した場合は、Streamlit Cloudのログを確認してください。

---

**作成日**: 2025年1月
**バージョン**: 1.0.0
